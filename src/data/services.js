import { Blocks, Divide, Sigma, ClipboardCheck, Video, MapPin } from 'lucide-react'

// Icons stay code-defined (not editable via the Visual Editor); title/text
// for each service now live in config.services.items, indexed to match.
export const SERVICE_ICONS = [Blocks, Divide, Sigma, ClipboardCheck, Video, MapPin]
